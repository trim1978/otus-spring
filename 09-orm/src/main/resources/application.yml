spring:
  datasource:
    url: jdbc:h2:mem:library
  sql:
    init:
      mode: always

  shell:
    interactive:
      enabled: true

  h2:
    console.enabled: true

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true

logging:
  level:
    ROOT: ERROR
